let resultado;

resultado = Math.PI;
resultado = Math.round(2.4);
resultado = Math.ceil(2.4); //redondea hacia arriba
resultado = Math.floor(2.9); //redondea hacia abajo
resultado = Math.abs(-2.9); //redondea hacia abajo
resultado = Math.min(5,7,1,2,4,0,-1); //redondea hacia abajo
resultado = Math.random(); //
resultado = Math.floor(Math.random() * 10); //



console.log(resultado);


const televisor = {
  modelo : 'vivoPlus',
  pulgadas : 50,
  precio : Math.floor(Math.random() * 500)
}

console.log(televisor);


const serHumano = {
  piernas : 2,
  ojos : 2,
  corazon : 1,
  sexo : 'Hombre',
  // cabello : true
}

console.log(serHumano);
